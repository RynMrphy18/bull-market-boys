<nav class="navbar navbar-expand-lg border-bottom-trans">
    <!-- Container wrapper -->
    <div class="container-fluid">
        <!-- Navbar brand -->
        <a class="navbar-brand" href="/">
            <span class="iconify-inline" data-icon="ri:exchange-dollar-fill"></span> 
            <span class="navbar-text">Fomo Mock Exchange</span>
        </a>

        <!-- Toggle button -->
        <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#toggleMobileMenu"
        aria-controls="toggleMobileMenu"
        aria-expanded="false"
        aria-label="Toggle navigation"
        >
        <i class="fas fa-bars" style="color: rgb(13, 110, 253);"></i>
        </button>

        <div class="collapse navbar-collapse" id="toggleMobileMenu">
            {{!-- this ul is here to space the nav bar, might find another solution idk --}}
            <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>

            <div class="d-flex justify-content-center">
                <i class="fas darkModeBtn btn btn-primary me-2"></i>
                {{#unless home}}
                {{#if loggedIn}}
                {{#unless dashboard}}
                <a class="btn btn-primary me-2" href="/dashboard" role="button">Dashboard</a>
                {{/unless}}
                <button id="logout" class="btn btn-outline-danger">Logout</button>
                {{else}}
                {{#unless signup}}
                <a href="/signup">
                    <button class="btn btn-outline-primary">Signup</button>
                </a>
                {{/unless}}
                {{#unless login}}
                <a href="/login">
                    <button class="btn btn-outline-primary">Login</button>
                </a>
                {{/unless}}
                {{/if}}
                {{/unless}}
            </div>
        </div>
    </div>
</nav>