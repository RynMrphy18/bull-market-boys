<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fomo Mock Exchange</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css" integrity="sha512-YWzhKL2whUzgiheMoBFwW8CKV4qpHQAEuvilg9FAn5VJUDwKZZxkJNuGM4XkWuk94WCrrwslk8yWNGmY1EduTA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <header class="container-fluid">
        <div class="row p-3">
            <div class="col-8">
                <a href="/">
                    <h1>
                        <span class="iconify-inline" data-icon="ri:exchange-dollar-fill"></span> Fomo Mock Exchange
                    </h1>
                </a>
            </div>
            <div class="col-4">
                <div class="position-relative">
                    <nav class="position-absolute top-0 end-0">
                        <i class="fas darkModeBtn btn btn-primary mt-2"></i>
                        {{#unless home}}
                        {{!-- <i class="fas darkModeBtn btn btn-primary mt-2"></i> --}}
                        {{#if loggedIn}}
                        {{#unless dashboard}}
                        <a href="/dashboard">
                            <div class="btn btn-primary mt-2">Dashboard</div>
                        </a>
                        {{/unless}}
                        <button id="logout" class="btn btn-outline-danger mt-2">Logout</button>
                        {{else}}
                        {{#unless signup}}
                        <a href="/signup">
                            <div class="btn btn-outline-primary mt-2">Signup</div>
                        </a>
                        {{/unless}}
                        {{#unless login}}
                        <a href="/login">
                            <div class="btn btn-primary mt-2">Login</div>
                        </a>
                        {{/unless}}
                        {{/if}}
                        {{/unless}}
                    </nav>
                </div>
            </div>
        </div>
    </header>
    {{{ body }}}
    <footer class="container-fluid text-center pt-3 fixed-bottom">
        <p class="fs-3">Thanks for visiting!</p>
    </footer>
    {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
<script src="/javascript/darkmode.js"></script>
<script src="https://code.iconify.design/2/2.0.3/iconify.min.js"></script>
</html>